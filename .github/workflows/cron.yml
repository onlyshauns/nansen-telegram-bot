name: Telegram Bot Cron

on:
  schedule:
    # News: 10pm SGT daily (14:00 UTC)
    - cron: '0 14 * * *'
    # Day A: 10am SGT on Sun, Mon, Wed (02:00 UTC)
    - cron: '0 2 * * 0,1,3'
    # Day B: 10am SGT on Tue, Thu, Sat (02:00 UTC)
    - cron: '0 2 * * 2,4,6'
    # Day C: 10am SGT on Fri (02:00 UTC)
    - cron: '0 2 * * 5'
  workflow_dispatch:
    inputs:
      endpoint:
        description: 'Endpoint to trigger'
        required: true
        type: choice
        options:
          - day-a
          - day-b
          - day-c
          - news

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Determine endpoint
        id: endpoint
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "path=${{ github.event.inputs.endpoint }}" >> $GITHUB_OUTPUT
          else
            CRON="${{ github.event.schedule }}"
            case "$CRON" in
              "0 14 * * *")     echo "path=news" >> $GITHUB_OUTPUT ;;
              "0 2 * * 0,1,3")  echo "path=day-a" >> $GITHUB_OUTPUT ;;
              "0 2 * * 2,4,6")  echo "path=day-b" >> $GITHUB_OUTPUT ;;
              "0 2 * * 5")      echo "path=day-c" >> $GITHUB_OUTPUT ;;
              *)                echo "path=unknown" >> $GITHUB_OUTPUT ;;
            esac
          fi

      - name: Trigger ${{ steps.endpoint.outputs.path }}
        if: steps.endpoint.outputs.path != 'unknown'
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "https://nansen-telegram-bot.vercel.app/api/generate/${{ steps.endpoint.outputs.path }}")

          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)

          echo "Status: $HTTP_CODE"
          echo "Response: $BODY"

          if [ "$HTTP_CODE" -ge 400 ]; then
            echo "::error::Request failed with status $HTTP_CODE"
            exit 1
          fi
